{
  "name": "zuroy-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test --project=api",
    "test:portal": "playwright test --project=browser-portal",
    "test:connect": "playwright test --project=browser-connect",
    "test:browser": "playwright test --project=browser-portal --project=browser-connect",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "dotenv": "^16",
    "typescript": "^5"
  }
}
